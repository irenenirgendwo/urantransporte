<%= form_for(@atomanlage) do |f| %>
  <% if @atomanlage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@atomanlage.errors.count, "error") %> prohibited this atomanlage from being saved:</h2>

      <ul>
      <% @atomanlage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :ort %><br>
    <%= f.text_field :ort %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
